import readme from "./readme.md";

export default {
  title: "web components/Delivery Day Zip Code Search",
  parameters: {
    notes: readme,
    layout: 'centered'
  }
};

const Template = (args) => {
  const zipcodeSearch = document.createElement('pn-zipcode-search');

  return zipcodeSearch;
};

export const ZipcodeSearch = Template.bind({});

