import { Component, h } from '@stencil/core';
import colors from '../../../assets/colors';
export class PnColors {
  render() {
    return (h("div", { class: "colors" },
      h("h1", null, "Colors"),
      h("div", { class: "palette-container" }, Object.entries(colors).map(([name, rgb]) => (h("div", { class: "color-palette" },
        h("div", { class: "color-swatch", style: { backgroundColor: rgb } }),
        h("h4", { class: "color-name" }, name),
        h("p", { class: "color-rg" }, rgb)))))));
  }
  static get is() { return "pn-colors"; }
  static get originalStyleUrls() { return {
    "$": ["pn-colors.scss"]
  }; }
  static get styleUrls() { return {
    "$": ["pn-colors.css"]
  }; }
}
