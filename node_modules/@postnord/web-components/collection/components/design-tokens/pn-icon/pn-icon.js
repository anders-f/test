import { Component, Prop, h, Host } from '@stencil/core';
import allIcons from '../../../assets/icons';
import colors from '../../../assets/colors';
export class PnIcon {
  constructor() {
    /** The name of the icon you want to use */
    this.symbol = null;
    /** Medium is default but you can pass "small" for a 16x16 icon */
    this.small = false;
    /** Check for the available colors in the knobs panel in the story */
    this.color = 'black';
  }
  getIcon() {
    // The replace is just setting the fill attribute on the SVG to the color passed to the component
    return (allIcons[this.symbol] || '').replace(/black/g, colors[this.color]);
  }
  render() {
    return h(Host, { class: `pn-icon ${this.small ? 'small' : ''}`, innerHTML: this.getIcon() });
  }
  static get is() { return "pn-icon"; }
  static get originalStyleUrls() { return {
    "$": ["pn-icon.scss"]
  }; }
  static get styleUrls() { return {
    "$": ["pn-icon.css"]
  }; }
  static get properties() { return {
    "symbol": {
      "type": "string",
      "mutable": false,
      "complexType": {
        "original": "string",
        "resolved": "string",
        "references": {}
      },
      "required": false,
      "optional": false,
      "docs": {
        "tags": [],
        "text": "The name of the icon you want to use"
      },
      "attribute": "symbol",
      "reflect": false,
      "defaultValue": "null"
    },
    "small": {
      "type": "boolean",
      "mutable": false,
      "complexType": {
        "original": "boolean",
        "resolved": "boolean",
        "references": {}
      },
      "required": false,
      "optional": false,
      "docs": {
        "tags": [],
        "text": "Medium is default but you can pass \"small\" for a 16x16 icon"
      },
      "attribute": "small",
      "reflect": false,
      "defaultValue": "false"
    },
    "color": {
      "type": "string",
      "mutable": false,
      "complexType": {
        "original": "string",
        "resolved": "string",
        "references": {}
      },
      "required": false,
      "optional": false,
      "docs": {
        "tags": [],
        "text": "Check for the available colors in the knobs panel in the story"
      },
      "attribute": "color",
      "reflect": false,
      "defaultValue": "'black'"
    }
  }; }
}
