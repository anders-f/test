import readme from './readme.md';

export default {
  title: 'navigation/page nav/Page Nav Item',
  parameters: {
    notes: readme
  }
};

const Template = () => {
  const text = document.createElement('h1');
  text.innerHTML = 'CHECK THE DOCS';

  return text;
};

export const PageNavItem = Template.bind({});