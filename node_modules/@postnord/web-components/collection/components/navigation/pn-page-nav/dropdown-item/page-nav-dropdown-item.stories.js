import readme from './readme.md';

export default {
  title: 'navigation/page nav/Dropdown Item',
  parameters: {
    notes: readme
  }
};

const Template = () => {
  const text = document.createElement('h1');
  text.innerHTML = 'CHECK THE DOCS';

  return text;
};

export const DropdownItem = Template.bind({});