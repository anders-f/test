export class PnFileUploadError extends Error {
  constructor(message, element, code = 'INVALID', data = null) {
    super(message);
    this.name = 'PNFileUploadError';
    this.element = element;
    this.code = code;
    this.data = data;
  }
}
