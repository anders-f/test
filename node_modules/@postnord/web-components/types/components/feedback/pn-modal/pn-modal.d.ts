import { EventEmitter } from '../../../stencil-public-runtime';
export declare class PnModal {
  /** Bind to this property if you want to control the visibility of the modal from your own data. */
  open: boolean;
  /** Event fired when the modal is closed, either by clicking outside or by triggering close button. */
  close: EventEmitter;
  focusableEls: Array<any>;
  hostElement: HTMLElement;
  mo: MutationObserver;
  untabbable: Array<any>;
  elToFocus: HTMLElement;
  componentDidLoad(): void;
  openHandler(): void;
  setFocusableElements(): void;
  addEventListeners(): void;
  removeEventListeners(): void;
  handleFocus: any;
  handleBlur: any;
  handleEsc: any;
  handleGlobalClick: any;
  focusHandler(e: any): void;
  blurHandler(e: any): void;
  escHandler({ code }: {
    code: any;
  }): void;
  globalClickHandler(e: any): void;
  render(): any;
}
