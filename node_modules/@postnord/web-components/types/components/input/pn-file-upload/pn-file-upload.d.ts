import { EventEmitter } from '../../../stencil-public-runtime';
import { PnUploadFileItem } from '../../../globals/types';
export declare class PnFileUpload {
  hostElement: HTMLElement;
  triggerElements: Array<HTMLElement>;
  renders: number;
  filesAdded: EventEmitter;
  update: EventEmitter;
  fileuploaderror: EventEmitter;
  valid: EventEmitter;
  uploadFile: EventEmitter;
  uploadCancelled: EventEmitter;
  uploadCompleted: EventEmitter;
  disabled: boolean;
  showSpinner: boolean;
  accept: string;
  maxSize: string;
  limit: number;
  multiple: boolean;
  fileInputElement: HTMLInputElement;
  invalid: boolean;
  files: Array<PnUploadFileItem>;
  maxSizeInBytes: number;
  componentWillLoad(): void;
  componentWillUpdate(): void;
  initMaxBytes(): void;
  componentDidLoad(): void;
  private _setDefaultValues;
  private _initTriggerElements;
  private _addFilesFromFileList;
  private _onAddFile;
  private _onDragOverFile;
  private _onDragLeaveFile;
  private _onDropFile;
  private _validateFileType;
  private _validateFileSize;
  private _validateLimit;
  private _isNotGreaterThanLimit;
  private _checkValidity;
  private _createBase64FromFile;
  private _addFile;
  private _removeFile;
  private _emitUpdateEvent;
  openFileExplorer(event: Event): void;
  startUpload(): Promise<void>;
  clearUpload(): Promise<void>;
  removeFile(targetFile: PnUploadFileItem): Promise<void>;
  updateFile(file: PnUploadFileItem, index?: any): Promise<void>;
  private _forceRender;
  render(): any;
}
