export declare class PnFooter {
  market: string;
  language: string;
  isLoggedIn: boolean;
  isBusinessUser: boolean;
  componentWillLoad(): void;
  init(): void;
  getTranslation(keyToMatch: string, languageToMatch: string): any;
  getUrl(urls: any, marketToMatch: string, languageToMatch: string): any;
  getDomain(urls: any, marketToMatch: string): any;
  render(): any;
  isEmpty(input: string): boolean;
}
