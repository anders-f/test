import { EventEmitter } from '../../../stencil-public-runtime';
export declare class PnZipCodeSearch {
  /** Event fired when search has result */
  searchsuccessful: EventEmitter;
  /** marketwebLanguage property will be prioritized before the state language */
  marketwebLanguage: string;
  /** If property marketwebLanguage is empty it will fall back on the state language */
  language: string;
  loading: boolean;
  error: boolean;
  errorMessage: string;
  showResult: boolean;
  zipCodeSearchResult: {
    delivery: string;
    upcoming: string;
    city: string;
    postalCode: string;
  };
  componentWillLoad(): void;
  init(): void;
  isEmpty(input: string): boolean;
  getTranslation(keyToMatch: string, languageToMatch: string): any;
  handleSearch(e: any): void;
  zipCodeSearch(zipCode: string): Promise<void>;
  validateZipCode(value: any): {
    error: boolean;
    value: any;
  };
  renderSearchResult(): any;
  renderErrorMessage(): any;
  render(): any;
}
